#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../ua-parser-js/script/cli.js" "$@"
else
  exec node  "$basedir/../../../../ua-parser-js/script/cli.js" "$@"
fi
# cmd-shim-target=/Users/fernandomac/Desktop/test/wdd/node_modules/.pnpm/@grafana+faro-web-sdk@1.19.0/node_modules/ua-parser-js/script/cli.js
